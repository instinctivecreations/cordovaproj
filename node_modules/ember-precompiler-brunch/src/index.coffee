fs = require 'fs'
jsdom = require 'jsdom'

module.exports = class EmberPrecompiler
  brunchPlugin: yes
  type: 'template'
  extension: 'hbs'
  precompile: yes

  setup: (@config) ->
    @window = jsdom.jsdom().createWindow()
    @window.run fs.readFileSync @config.files.templates.paths.jquery, 'utf8'
    @window.run fs.readFileSync @config.files.templates.paths.handlebars, 'utf8'
    @window.run fs.readFileSync @config.files.templates.paths.ember, 'utf8'

  constructor: (@config) ->
    if @config.files.templates?.paths?
      @setup(@config)
    null

  compile: (data, path, callback) ->
    if not @window?
      return callback "files.templates.paths must be set in your config", {}
    try
      content = @precompileEmber data
      path = path
        .replace(new RegExp('\\\\', 'g'), '/')
        .replace(/^app\//, '')
        .replace(/^templates\//, '')
        .replace(/\.\w+$/, '')
      result = "Ember.TEMPLATES[#{JSON.stringify(path)}] = Ember.Handlebars.template(#{content});\nmodule.exports = module.id;"
    catch e
      error = e
    finally
      callback error, result

  precompileEmber: (data) ->
    @window.Ember.Handlebars.precompile(data)

